version: "3.9"

services:
  api:
    build: ./api
    ports:
      - "3001:3000"
    container_name: pwa_api

  web:
    build: ./web
    ports:
      - "3000:3000"
    depends_on:
      - api
    container_name: pwa_web
